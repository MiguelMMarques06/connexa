# Arquivo de exemplo de variáveis de ambiente para produção
# COPIE este arquivo para .env.production e configure os valores

# IMPORTANTE: Nunca commitee este arquivo com valores reais no git!
# Adicione .env.* no .gitignore

# ==============================================
# CONFIGURAÇÕES DE SEGURANÇA
# ==============================================

# Chave de criptografia para armazenamento local (OBRIGATÓRIO em produção)
# Gere uma chave segura de 32+ caracteres usando: openssl rand -base64 32
REACT_APP_ENCRYPTION_KEY=your-super-secure-encryption-key-here-32-chars-min

# URL da API (sem barra no final)
REACT_APP_API_URL=https://api.connexa.com

# Environment
REACT_APP_ENVIRONMENT=production

# ==============================================
# CONFIGURAÇÕES OPCIONAIS
# ==============================================

# Timeout para requisições API (em milissegundos)
REACT_APP_API_TIMEOUT=10000

# Intervalo de verificação de token (em milissegundos)
REACT_APP_TOKEN_CHECK_INTERVAL=60000

# Tempo antes da expiração para renovar token (em segundos)
REACT_APP_TOKEN_REFRESH_THRESHOLD=300

# Habilitar logs de desenvolvimento (apenas para debug)
REACT_APP_DEBUG_MODE=false

# ==============================================
# CONFIGURAÇÕES DE MONITORAMENTO
# ==============================================

# Sentry DSN para monitoramento de erros (opcional)
REACT_APP_SENTRY_DSN=

# Google Analytics ID (opcional)
REACT_APP_GA_ID=

# ==============================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ==============================================

# 1. DESENVOLVIMENTO:
#    - Copie este arquivo para .env.local
#    - Configure REACT_APP_API_URL=http://localhost:3001
#    - Use uma chave de desenvolvimento para REACT_APP_ENCRYPTION_KEY

# 2. PRODUÇÃO:
#    - Copie este arquivo para .env.production
#    - Configure todas as variáveis com valores de produção
#    - Use HTTPS para REACT_APP_API_URL
#    - Gere uma chave de criptografia forte e única

# 3. STAGING:
#    - Copie este arquivo para .env.staging
#    - Configure com valores de teste/staging

# ==============================================
# VERIFICAÇÃO DE SEGURANÇA
# ==============================================

# Antes de fazer deploy, verifique:
# [ ] REACT_APP_ENCRYPTION_KEY é única e segura (32+ caracteres)
# [ ] REACT_APP_API_URL usa HTTPS em produção
# [ ] Não há informações sensíveis expostas
# [ ] Arquivo .env.* está no .gitignore
# [ ] Variáveis estão configuradas no servidor/CI/CD

# ==============================================
# COMANDOS ÚTEIS
# ==============================================

# Gerar chave de criptografia segura:
# openssl rand -base64 32

# Verificar variáveis carregadas:
# npm run build (verifica se todas as variáveis REACT_APP_* estão definidas)

# Testar em modo de produção local:
# npm run build && npm install -g serve && serve -s build